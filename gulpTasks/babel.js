'use strict';

var gulp = require('gulp');
var clean = require('gulp-clean');

gulp.task('babel', function() {
    // return gulp.src('dist', {
    //         read: false
    //     })
    //     .pipe(clean());
});